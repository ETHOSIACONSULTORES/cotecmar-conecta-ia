const f=document.querySelector('#loginForm'),err=document.querySelector('#loginError');
f.addEventListener('submit',async e=>{e.preventDefault();err.textContent='';const b=f.querySelector('button');b.disabled=true;b.textContent='Validando…';try{const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:document.querySelector('#password').value})});const d=await r.json();if(!r.ok)throw new Error(d.detail||'No autorizado');location.href='/admin'}catch(x){err.textContent=x.message;b.disabled=false;b.textContent='Ingresar →'}});
